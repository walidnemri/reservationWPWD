{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>{{ titre }}</h1>

    <ul>
    {% for artist in artists %}
        <li>{{ artist }}</li>
    {% endfor %}
    </ul>
</div>
{% endblock %}
