{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>{{ titre }}</h1>

    <p>{{ artist }}</p>
    
    {% for artistType in artist.ArtistTypes  %}
        <li>{{ artistType.type }}</li>
    {% endfor %}
</div>
{% endblock %}
